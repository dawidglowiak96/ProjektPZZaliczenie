@model CarDetailsServiceModel

<div class="col-md-6 offset-3">
    <h2>@Model.Brand @Model.Model</h2>
    <p>@Model.Description </p>
    <img class="d-block w-100" src="@Model.ImageUrl" alt="@Model.Brand @Model.Model">

    <p>@Model.Year: Year of manufacture <p>
        <div class="row">
            <div class="col-12 text-right">
            </div>
        </div>        
        <button class="btn btn-primary" id="Clear-button">Clear</button>
    </div>
